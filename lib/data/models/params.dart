// import '../../domain/entities/user.dart';
// import '../../domain/entities/accounts/account.dart';
// import '../../domain/entities/accounts/account_savings.dart';
// import '../../domain/entities/accounts/account_simple_savings.dart';
// import '../../domain/entities/category.dart';
// import '../../domain/entities/recurrence.dart';
// import '../../domain/entities/transaction.dart';
// import '../../domain/entities/transfer.dart';

class Params {
  // Account? account;
  // int? id, id2, id3;
  // Category? category;
  // Recurrence? recurrence;
  // AccountSavings? accountSavings;
  // AccountSimpleSavings? accountSimpleSavings;
  // List<String>? tableNames;
  // DateTime? past;
  // DateTime? future;
  // Transaction? transaction;
  // Transfer? transfer;
  // User? user;
  // Params.endDate({this.id, this.id2, this.future});
  // Params.id({this.id});
  // Params.id2({this.id, this.id2});
  // Params.id3({this.id, this.id2, this.id3});
  // Params.category({this.category});
  // Params.recurrence({this.recurrence});
  // Params.transaction({this.transaction});
  // Params.tableNames({this.tableNames});
  // Params.transfer({this.transfer});
  // Params.date({this.past, this.future});
  // Params.user({this.user});
  // Params.accountSavings({this.accountSavings});
  // Params.accountSimpleSavings({this.accountSimpleSavings});
  // Params.shareAccount({this.account, this.id});
  // Params({this.account});
}
