import 'package:flutter/material.dart';

class Recurrence {
  int id;
  String title;
  Duration? duration;
  IconData icon;
  Recurrence({
    required this.id,
    required this.title,
    required this.duration,
    required this.icon,
  });
}
