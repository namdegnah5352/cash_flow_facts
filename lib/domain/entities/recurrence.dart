class Recurrence {
  int id;
  String title;
  Duration? duration;
  String iconPath;
  Recurrence({
    required this.id,
    required this.title,
    required this.duration,
    required this.iconPath,
  });
}
